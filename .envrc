# The flake dev shell already provides mise and puts the pinned toolchain
# (mise.toml) on PATH, so direnv only needs to enter the flake. Do not add
# `use mise`: it is deprecated in mise's direnv integration and shadows the
# PATH that `use flake` exports.
use flake
